# Copy to .env and fill in. Never commit .env.

# Hugging Face repo with the fine-tuned classifier (Person B uploads it)
CLASSIFIER_REPO=<hf-username>/mapens-classifier
# Only needed for uploading or for a private HF repo
HF_TOKEN=

# LLM API key for scripts/llm_label.py (only Person B needs it)
LLM_API_KEY=
