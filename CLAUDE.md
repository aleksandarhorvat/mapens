# MapeNS

Read MAPENS_CONTEXT.md before doing anything in this repo. It holds the project description,
the data and API contracts, the work split between Person A and Person B, and the rules you must follow.

Short version:

- Ask the user whether they are Person A or Person B, and work only on that person's files (section 9).
- Never change the contracts in section 6 without saying so explicitly.
- Follow the assistant rules in section 11 and the writing style rules in section 14
  (plain ASCII, no em dashes, no emoji, no filler words).
- Run `python scripts/check_style.py` before suggesting a commit.
- Check PROGRESS.md to see what the other person has finished.
