server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # Same /api prefix as the Vite dev proxy
  location /api/ {
    proxy_pass http://backend:8000/;
    proxy_set_header Host $host;
  }

  location / {
    try_files $uri /index.html;
  }
}
